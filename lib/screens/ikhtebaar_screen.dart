import 'package:flutter/material.dart';

class IkhtebaarScreen extends StatelessWidget {
  static const routeName = '/ikhtebaar-screen'; 
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Text('This is the ikhtebaar Screen'),
      ),
    );
  }
}
